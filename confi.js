
const config = {
    db : {
        host: "localhost",
        user: "root",
        password: "root",
        database : "craftdinner",// mamp ; user = root, password = root
}};

module.exports = config;